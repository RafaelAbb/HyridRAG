[pytest]
testpaths = tests
pythonpath = .
markers =
    llm_eval: makes real LLM API calls (slow, costs money, non-deterministic) - excluded by default, run explicitly with -m llm_eval
addopts = -m "not llm_eval"
